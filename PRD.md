# Curio —— PRD v0.3

> **版本**：v0.3（搜寻型 Agent，重大定位修正）
> **作者**：周小丁
> **日期**：2026-05-29
> **状态**：M0 设计中
> **重大变更**：v0.2 → v0.3，**从订阅型 Agent 改为搜寻型 Agent**
>   - 用户不再管理 KOL 订阅列表
>   - Agent 主动从全网（M0 中文主场）搜索领域内有价值的内容
>   - KOL 降级为内容元数据，不再是用户配置项

---

## 〇、版本变更说明（重要）

### v0.2 vs v0.3 的核心差别

| 维度 | v0.2（订阅型） | **v0.3（搜寻型）** |
|---|---|---|
| 核心动作 | 用户给主题 → AI 推 KOL → 用户勾选 → 抓已订阅 KOL 内容 | 用户给领域 → **Agent 直接全网搜该领域有价值内容 → 推送** |
| 信源 | 封闭：你订了谁就给你看谁 | **开放：任何相关有价值的源都可能进来** |
| 用户负担 | 维护订阅列表 | 只维护"我关心什么领域" |
| KOL 角色 | 核心实体 | **降级为元数据**（内容上的 source 字段） |
| 类比 | RSS / Feedly | **Google News / 经济学人编辑部 / 私人主编** |

### 为什么改

1. **用户门槛太高**：让用户管 KOL 订阅，等于把"信息策展"工作量转嫁回用户
2. **真正的 Agent 心智**：Agent 应该像主编替你找好东西，而不是只整理你已选的源
3. **不会漏掉非 KOL 来源**：很多重要新闻来自不知名媒体/小博主/突发新闻，订阅型必漏
4. **冷启动顺**：新用户立刻有内容看，不用先建订阅库

---

## 一、一句话定位

> **你给一个领域，Agent 当你的私人主编：从全网找有价值的内容，每周/每天替你精选一份。**

类比：**经济学人 + 个性化 + 解释性**——它是"为你一个人服务的领域主编"。

---

## 二、目标用户

M0 单机自用（周小丁本人），M2 后预留多用户能力。

| 用户类型 | 痛点 |
|---|---|
| **信息焦虑型** | 想关注但不知道从哪开始，又怕错过 |
| **多领域学习者** | AI / 金融 / 半导体并行关注，时间分裂 |
| **创作者** | 需要素材但不想自己挖 |

**用户**说一句话就够：「**我关心 X**」——剩下都是 Agent 的事。

---

## 三、产品的"灵魂"（如果只能保一件事）

**Profile（你是谁）↔ Feedback（你的反馈）↔ Agent（搜什么 + 留什么）的回路。**

```
你的 Profile（关心 / 看不上 / 想要的信号 / 节奏）
    ↓ 驱动
Agent 在领域内搜索 + 打分 + 选内容
    ↓ 推送给你
你看完写一段反馈
    ↓ 摘要
回写 Profile
    ↑
   （回路）
```

**这条回路在跑，Curio 才是 Agent；不在跑，它就是高级新闻聚合器。**

---

## 四、核心价值（vs 已有方案）

| 方案 | 不足 | Curio 的差异 |
|---|---|---|
| **RSS 阅读器** | 你给信源，按时间排序 | **你给领域，Agent 给信源**，按重要性排序 |
| **Refind / Pocket Discover** | 黑盒推荐 | **每条都给"为什么推"** |
| **小红书 / 抖音算法** | 黑盒，画像在平台手里 | **画像在你手里，可见可改** |
| **张咋啦 Newsletter** | 工具型，需手动配置 | **Agent 自主决策** |
| **Starfan 个人雷达** | 同思路、自然语言画像 | 我们差异化：**多 Domain + 可解释 + Web 产品** |

差异化锚点：**搜寻型 / 可解释 / 多 Domain / 反馈闭环**

---

## 五、Agent 的 4 个动作

| # | 动作 | 输入 | 输出 |
|---|---|---|---|
| 1 | **拆领域** | "vibe coding" | 4 子话题 + 关键词列表（搜索用）|
| 2 | **全网搜+抓** | 子话题关键词 + Profile | 候选内容池（30-100 条/期）|
| 3 | **打分排序** | 候选池 + Profile + 历史反馈 | 必读 / 参考 / 跳过 + 每条"为什么推" |
| 4 | **写主编开场白** | 当期入选内容 | "本周最大事件是 X，考虑你最近反馈，必读区放了 3 条 Y 类内容" |

**M0 上半场已验证动作 1**（CLI 推 KOL 改成"拆领域 + 关键词"即可）；动作 2-4 是 M0 下半场的核心。

---

## 六、信源策略：**推荐订阅 + 全网补充**（C 方案）

### 6.1 三类信源并行

```
┌─────────────────────────────────────────────┐
│ 1. 用户"钉住"的源（pinned，可选）            │ ← 强提升优先级
│    onboarding 时 Agent 推 5-8 个底底源       │
│    用户勾几个特别想盯的（不勾也行）           │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│ 2. 全网搜索（broad search）                  │ ← 主战场
│    Agent 用关键词在多平台搜索                │
│    M0 中文主场：B 站搜索 + 小宇宙搜索 + RSS │
└─────────────────────────────────────────────┘
┌─────────────────────────────────────────────┐
│ 3. 平台热门监测（hot scan，M1+）              │ ← 冷启动福音
│    B 站知识区热榜、HN、Reddit r/MachineLearning│
└─────────────────────────────────────────────┘
```

### 6.2 M0 抓取范围（中文优先）

| 平台 | 方式 | 优先级 |
|---|---|---|
| **B 站** | 搜索关键词，按"最新+最热"取 Top N | P0 |
| **小宇宙** | 搜索关键词或 RSS 订阅 | P0 |
| **中文 RSS 博客** | content-catcher 现有能力 | P0 |
| YouTube | 关键词搜索 | P1（账号限制问题，可选）|
| 微信公众号 | 暂不抓（合规复杂） | M2+ |
| Twitter/X | 暂不抓（API 限制） | M2+ |

### 6.3 KOL 在新模型里的位置

KOL **依然存在**，但**不是用户配置项**：
- 一条 Content 上有 `source` 字段记录来源
- 系统统计哪些源持续产出高分内容（隐式信任度）
- 用户可以在内容详情页"钉住"或"屏蔽"某个源（轻交互）
- **不需要"管理 KOL 列表"这个功能**

---

## 七、数据模型（v0.3 重写）

### 7.1 四个核心实体

```
Profile（个人画像）—— 全局唯一
├── identity        "电子信息工程大四 + 搜狗实习生 + AI 重度用户"
├── interests       ["vibe coding", "AI Agent", "美联储", ...]
├── dislikes        ["纯流量号", "标题党", "口水内容"]
├── signal_preferences  ["要工程实践", "要代码示例", "拒绝玄学"]
├── reading_pace    "工作日 30 分钟，周末 2 小时"
└── auto_updated    bool

Domain（领域）—— 用户聚焦的赛道
├── id              "ai" / "finance" / "semiconductor"
├── name            "AI" / "金融" / "半导体"
├── icon
├── description
├── source          preset / custom
├── frequency       daily / weekly
├── topic_ids       [Topic.id]
└── status          active / paused

Topic（话题）—— Domain 下具体关注点
├── id              "vibe-coding"
├── name            "Vibe Coding"
├── domain_id       "ai"
├── subtopics       ["Claude Code", "Cursor", "Side Project", "Agent 工具"]
├── search_keywords ["vibe coding", "Claude Code 实战", "AI 编程"]   ← 新增
├── pinned_sources  [{source_type, identifier, label}]              ← 新增（用户钉住的）
├── blocked_sources [{...}]                                          ← 新增（用户屏蔽的）
└── frequency       inherit / daily / weekly

Content（一条内容）—— 主角
├── id
├── topic_ids       [Topic.id]
├── domain_ids      [Domain.id]
├── title
├── url
├── source          { type: "bilibili", id: "...", name: "大牙大-" }   ← KOL 降级为这里
├── source_kind     pinned / search / hot       ← 来自哪类信源
├── published_at
├── duration_sec
├── summary
├── score           必读 / 参考 / 跳过
├── score_detail    {新颖度, 深度, 相关度} 1-10
└── why_recommend   "因为提到 Claude Code 1.0 发布"

Feedback（反馈）—— 单期 Digest 的反馈
├── digest_id
├── content_id      可选（针对单条）
├── kind            useful / boring / wrong_topic / want_more
└── note            自由文本

Digest（一期推送）—— 兼容日报/周报
├── id              "2026-05-29-ai" / "2026-W22-ai"
├── frequency       daily / weekly
├── scope           domain / topic
├── period
├── must_read       [Content.id]
├── reference       [Content.id]
├── skip            [Content.id]
├── intro           Agent 写的开场白
├── feedback_section_md
└── sent_at
```

### 7.2 关键变化（vs v0.2）

| | v0.2 | v0.3 |
|---|---|---|
| KOL 实体 | 一级实体，用户配置 | **取消**，降级为 Content.source |
| Topic 字段 | kols（用户订阅的 KOL 列表）| **search_keywords + pinned_sources + blocked_sources** |
| 用户主路径 | 探索 → 推 KOL → 勾选订阅 | **探索 → 立即出第一份精选** |

---

## 八、用户旅程（v0.3 主线）

### 旅程一：第一次用 Curio
```
Day 0
你：在首页点 "+ 新领域" 输入 "vibe coding"
Agent：拆 4 子话题 + 列搜索关键词
       Onboarding 推荐：「这几个源在 vibe coding 圈很活跃，要不要先钉住？」
       （大牙大 / Latent Space / AI Engineer 等 5-8 个，可选）
你：可以全跳过，也可以钉 1-2 个
Agent："好，开始第一次抓取..."
       几分钟后 → 跳转到第一份 Digest
你：看到 必读 3 条 + 参考 5 条 + 跳过 12 条
    每条都有"为什么推"
    末尾反馈区已经准备好
```

### 旅程二：日复一日
```
每天/每周（按 Domain 频率）
邮件到了：
  "📨 AI · 周报 · 5/25-31
   主编开场白：本周最大事件是 Claude Code 1.0 发布。考虑到你上周
              反馈想多看实战，必读区全是工程演示。"
你：点开邮件 → 必读 3 条挨条读
    在反馈区写："想多看 Claude Code 实战，少看抽象方法论"
Agent：摘要这段反馈 → 写到 Profile → 下次推送参考
```

### 旅程三：内容级控制（轻量）
```
某一天你看到一条内容："XX UP 主又水了"
你：在内容详情页点 "屏蔽这个源"
Agent：把该源加入当前 Topic 的 blocked_sources
       下次抓取自动跳过
```

---

## 九、产品架构

```
┌──────────────────────────────────────────┐
│  Web UI（M2+）                             │ ← Wireframe v2 已冻结
├──────────────────────────────────────────┤
│  API 层（M1 FastAPI）                      │
├──────────────────────────────────────────┤
│  Agent 大脑（M0 = WorkBuddy 手动）         │
│  - 拆领域 / 关键词                          │
│  - 全网搜（关键词 → 候选池）                │
│  - 打分排序                                 │
│  - 写开场白 + 摘要反馈回写 Profile          │
├──────────────────────────────────────────┤
│  采集层（M0 利用 content-catcher 局部能力） │
│  - 平台搜索 API（B 站搜索接口、小宇宙...）  │
│  - RSS 拉取                                │
│  - 视频/音频转写                            │
└──────────────────────────────────────────┘
```

---

## 十、M0 范围（功能优先版）

### M0 目标：跑通搜寻型 Agent 最薄闭环

```
1 Domain（AI）+ 1 Topic（vibe coding）+ 1 信源（B 站搜索）
↓
Agent 拆主题 → 关键词
↓
脚本调 B 站搜索接口 → 拉 30-50 条候选
↓
Agent 打分 + 写理由（M0 用 WorkBuddy 手动）
↓
输出 markdown digest（终端 + 文件）
↓
人工读 → 在文件末尾写一段反馈
↓
Agent 读反馈 → 摘要写 Profile
```

**M0 验收**：跑一遍能输出一份让我**真的愿意读完**的 markdown。

### M0 不做

| 功能 | 推迟到 |
|---|---|
| Web UI | 已冻结到 M2 |
| 多 Domain 并行 | M1 |
| 真邮件发送 | M1 |
| 多平台抓取（小宇宙 / RSS）| M1 |
| 反馈→画像自动回写 | M1（M0 手动） |
| YouTube / 公众号 / X | M2+ |

---

## 十一、里程碑（v0.3）

| 阶段 | 目标 |
|---|---|
| **M0** ← 当前 | AI/vibe coding 单 Topic 单源（B 站搜索）跑通最小闭环 |
| **M1** | 多 Topic + 多源（B 站 + 小宇宙 + RSS）+ 真邮件 + Web UI 解冻 |
| **M2** | 多 Domain + Web UI 上线 + 反馈闭环自动化 |
| **M3** | 打磨 + GitHub 开源 + 写故事 |

---

## 十二、北极星问题（v0.3 修订）

1. **搜索质量**：B 站搜"vibe coding"返回的 30 条，AI 能不能靠 Profile + 标题摘要选出 3 条好的？
2. **冷启动**：第一次用没历史反馈，Profile 只有 onboarding 填的几行，Agent 能多准？
3. **去重**：跨平台同一事件多条报道，Agent 能合并吗？
4. **echo chamber**：画像越准 → Agent 越不推"你不爱看但应该看"的内容，怎么破？
5. **诚实边界**：搜不到好内容时，Agent 应该坦然说"本周没好东西" vs 凑数？

---

## 十三、附

- **历史 PRD**：`PRD.v0.2-subscribe.md`（订阅型，已废弃）/ `PRD.cli-v0.md`（CLI 历史）
- **CLI demo**：`curator.py`（M0 上半场，仍可用作 prompt 实验台）
- **Wireframe**：`web/`（Curio v2 Linear 风，已冻结到 M2）
